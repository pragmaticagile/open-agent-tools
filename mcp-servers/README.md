# MCP Servers

This folder contains public Model Context Protocol servers.

Each server should be independently usable and documented.

Current servers:

- `ado-plus-mcp` - Azure DevOps read-only MCP server.

Recommended structure for each server:

```text
server-name/
  README.md
  LICENSE
  .env.example
  package.json
  src/
```
